apiVersion: actions.summerwind.dev/v1alpha1
kind: RunnerDeployment
metadata:
  name: my-gh-runner
  namespace: arc
spec:
  replicas: 2
  template:
    spec:
      repository: CMelerom/spring-petclinic-rest
      labels:
        - helm-gh-runner
      dockerEnabled: true